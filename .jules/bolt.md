## 2024-05-15 - React Default Tests
**Learning:** Default Create React App tests check for "learn react" text, which fails if the default App template is replaced by a custom application structure.
**Action:** Always verify test files when optimizing an existing React codebase, as the default boilerplate tests might be outdated and broken, needing fixes to allow testing pipeline to pass.
